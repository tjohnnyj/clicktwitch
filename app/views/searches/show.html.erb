<p id="notice"><%= notice %></p>

<p>
  <strong>Term:</strong>
  <%= current_user.inspect %>
  <%= @search.term %>
  <% tweets = @search.query_twitter(@search.term) %>
  <%= tweets %>
</p>

<%= link_to 'Edit', edit_search_path(@search) %> |
<%= link_to 'Back', searches_path %>
